{
  "name": "selenium-playwright-migration-toolkit",
  "version": "4.0.0",
  "description": "Selenium to Playwright-BDD migration toolkit with bddgen support",
  "scripts": {
    "bddgen": "npx bddgen",
    "test": "npm run bddgen && npx playwright test",
    "test:headed": "npm run bddgen && npx playwright test --headed",
    "test:debug": "npm run bddgen && npx playwright test --debug",
    "test:ui": "npm run bddgen && npx playwright test --ui",
    "report": "npx playwright show-report",
    "login": "node scripts/secure-login.js",
    "generate-creds": "node scripts/secure-login.js --generate",
    "check-session": "node scripts/secure-login.js --check",
    "force-login": "node scripts/secure-login.js --force"
  },
  "dependencies": {
    "playwright": "^1.40.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright-bdd": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
